model User {
  id        String   @id @default(cuid())
  name      String?
  email     String   @unique
  password  String
  balance   Int      @default(0)  // store in kobo (₦)
  numbers   PurchasedNumber[]
}

model PurchasedNumber {
  id          String @id @default(cuid())
  phoneNumber String
  sid         String
  user        User   @relation(fields: [userId], references: [id])
  userId      String
  createdAt   DateTime @default(now())
}
